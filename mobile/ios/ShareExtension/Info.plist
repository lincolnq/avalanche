<?xml version="1.0" encoding="UTF-8"?>
<!--
Share extension Info.plist (docs/35). NSExtension can't be expressed via
INFOPLIST_KEY_* build settings, so it lives here; the Apple-defined bundle keys
are merged in at build time from GENERATE_INFOPLIST_FILE in project.yml.

The activation rule accepts a single image, matching the images-only scope.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
		<key>NSExtensionAttributes</key>
		<dict>
			<key>NSExtensionActivationRule</key>
			<dict>
				<key>NSExtensionActivationSupportsImageWithMaxCount</key>
				<integer>1</integer>
			</dict>
		</dict>
	</dict>
</dict>
</plist>
