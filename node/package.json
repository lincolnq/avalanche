{
  "name": "actnet-node-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @actnet/app-core",
    "build:debug": "npm run build:debug -w @actnet/app-core",
    "docs": "npm run docs -w @actnet/app-core"
  },
  "engines": {
    "node": ">=26"
  }
}
